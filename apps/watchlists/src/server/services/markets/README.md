Typed Morpho API client, query documents, and service methods live here.

- `client.ts`: GraphQL POST transport and upstream error handling.
- `queries/`: one Morpho GraphQL operation per file.
- `service/`: one app-facing Morpho API method per file, returning the selected Morpho query output directly.
